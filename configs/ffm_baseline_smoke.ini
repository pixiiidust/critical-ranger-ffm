# Part C0.1 unmanaged FFM baseline smoke config
# Frozen arena constants are shared by baseline and agent — do not change between comparison runs.
# Measurement numbers carried forward must come from grid_width/grid_height >= min_gate_grid_size.
grid_width=128
grid_height=128
min_gate_grid_size=128
p=0.01
f=0.000001
seed=20260609
initial_tree_density=0.55
warmup_steps=10000
cluster_target=2000
max_steps=1500000
connectivity=4
schema_version=1
run_id=part-c0-baseline-smoke
out=data/fixtures/part_c0_baseline_clusters.csv
summary=reports/part-c0-baseline-smoke/summary.json
min_orders_of_magnitude=1.5
overlap_warn_rate=0.10
